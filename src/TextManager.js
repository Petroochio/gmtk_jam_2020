class TextManager {
  constructor() {
    this.text = [
      'This is some test text.',
      `We gotta find something that's funny to type, but still a little boring and also not racist`,
    ];

    this.currentLine = 0;

  }

  sendKey() {
    
  }
}

export default TextManager;   
